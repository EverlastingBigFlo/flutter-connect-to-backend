import 'package:flutter/material.dart';

class MyText extends StatelessWidget {

  final Color color;
  final double fontSize;
  final FontWeight fontWeight;
  final FontWeight fontweight;
  
  const MyText({super.key, required this.color, required this.fontSize, required this.fontWeight, required this.fontweight});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}